@{
    ViewData["Title"] = "Home Page";
}
<dl>
    @if (User.Claims.Any())
    {
        @foreach (var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>
        }
    }
    else
    {

    }
</dl>